require "rails_helper"

RSpec.describe "Create Action", type: :feature do
  context "with valid attributes" do
    xit "will create an action" do
    end
  end

  context "with invalid attributes" do
    xit "will not create an action" do
    end
  end
end
